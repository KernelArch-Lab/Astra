% =============================================================================
% papers/paper1/numbers/numbers.tex
%
% Placeholder. Regenerated from artefact CSVs by
%   scripts/gen_numbers_tex.py
% which is invoked by
%   papers/paper1/build.sh --refresh
%
% While this file is empty, the \todoNum{...} placeholders in macros.tex
% remain visible in the PDF as red [validate p99 ns]-style markers — so
% reviewers (and authors) can see at a glance what is unfilled.
%
% Do NOT hand-edit \renewcommand lines here — they will be overwritten
% on the next sweep. If you want to override a single number for a
% sub-deadline iteration, add the \renewcommand to macros.tex AFTER
% the \newcommand, not here.
% =============================================================================
